import { Component } from '@angular/core';
import { Landen } from './landen/landen';

@Component({
  imports: [Landen],
  selector: 'app-root',
  styleUrl: './app.css',
  templateUrl: './app.html',
})
export class App {
}
